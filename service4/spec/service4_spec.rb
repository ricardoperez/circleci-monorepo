RSpec.describe Service4 do
  it "has a version number" do
    expect(Service4::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end

  it "does something not useful" do
    expect(true).to eq(true)
  end
end
